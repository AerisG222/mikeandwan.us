import {
    describe,
    ddescribe,
    expect,
    iit,
    it
} from '@angular/core/testing';
import {PhotoSource} from './photo-source.model';

describe('PhotoSource', () => {
    /*
    it('should create an instance', () => {
      expect(new PhotoSource()).toBeTruthy();
    });
    */
    expect(true).toBeTruthy();
});
