import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {DialogButton} from './dialog-button';

describe('DialogButton', () => {
  /*
  it('should create an instance', () => {
    expect(new DialogButton()).toBeTruthy();
  });
  */
  expect(true).toBeTruthy();
});
