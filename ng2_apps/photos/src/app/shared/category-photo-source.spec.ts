import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {CategoryPhotoSource} from './category-photo-source';

describe('CategoryPhotoSource', () => {
  /*
  it('should create an instance', () => {
    expect(new CategoryPhotoSource()).toBeTruthy();
  });
  */
  expect(true).toBeTruthy();
});
