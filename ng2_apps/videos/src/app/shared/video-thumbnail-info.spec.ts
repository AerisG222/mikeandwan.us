import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {VideoThumbnailInfo} from './video-thumbnail-info';

describe('VideoThumbnailInfo', () => {
  /*
  it('should create an instance', () => {
    expect(new VideoThumbnailInfo()).toBeTruthy();
  });
  */
  expect(true).toBeTruthy();
});
