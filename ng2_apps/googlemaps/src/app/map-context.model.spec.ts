import {
    describe,
    ddescribe,
    expect,
    iit,
    it
} from '@angular/core/testing';
import {MapContext} from './map-context.model';

describe('MapContext', () => {
    /*
      it('should create an instance', () => {
        expect(new MapContext()).toBeTruthy();
      });
    */
    expect(true).toBeTruthy();
});
