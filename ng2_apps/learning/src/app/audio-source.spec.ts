import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {AudioSource} from './audio-source';

describe('AudioSource', () => {
  /*
  it('should create an instance', () => {
    expect(new AudioSource()).toBeTruthy();
  });
  */
  expect(true).toBeTruthy();
});
