import {
    describe,
    ddescribe,
    expect,
    iit,
    it
} from '@angular/core/testing';
import {BoardSector} from './board-sector.model';

describe('BoardSector', () => {
    /*
    it('should create an instance', () => {
      expect(new BoardSector()).toBeTruthy();
    });
    */
    expect(true).toBeTruthy();
});
