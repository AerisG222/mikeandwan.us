import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {CategoryThumbnailInfo} from './category-thumbnail-info.model';

describe('CategoryThumbnailInfo', () => {
  /*
  it('should create an instance', () => {
    expect(new CategoryThumbnailInfo()).toBeTruthy();
  });
  */
  expect(true).toBeTruthy();
});
