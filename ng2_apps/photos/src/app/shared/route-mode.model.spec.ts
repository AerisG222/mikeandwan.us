import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {RouteMode} from './route-mode.model';

describe('RouteMode', () => {
  /*
  it('should create an instance', () => {
    expect(new RouteMode()).toBeTruthy();
  });
  */
  expect(true).toBeTruthy();
});
