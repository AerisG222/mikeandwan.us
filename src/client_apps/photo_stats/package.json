{
  "name": "photo_stats",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "ts-loader": "^4.3.0",
    "typescript": "^2.8.3",
    "webpack": "^4.8.3",
    "webpack-bundle-analyzer": "^2.12.0",
    "webpack-cli": "^2.1.3",
    "webpack-stats-plugin": "^0.2.1"
  },
  "scripts": {
    "dev_build": "rm -rf dist && mkdir dist && webpack",
    "prod_build": "rm -rf dist && mkdir dist && webpack -p --config ./webpack.production.config.js",
    "bundle-report": "webpack-bundle-analyzer dist/stats.json"
  },
  "dependencies": {
    "d3": "5.4.0",
    "oidc-client": "1.5.0-beta.3"
  }
}
