import {
    describe,
    ddescribe,
    expect,
    iit,
    it
} from '@angular/core/testing';
import {ExifFormatter} from './exif-formatter.model';

describe('ExifFormatter', () => {
    /*
    it('should create an instance', () => {
      expect(new ExifFormatter()).toBeTruthy();
    });
    */
    expect(true).toBeTruthy();
});
