import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {SizeInfo} from './size-info';

describe('SizeInfo', () => {
  /*
  it('should create an instance', () => {
    expect(new SizeInfo()).toBeTruthy();
  });
  */
  expect(true).toBeTruthy();
});
