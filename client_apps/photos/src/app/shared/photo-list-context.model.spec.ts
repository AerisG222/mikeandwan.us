import {
    describe,
    ddescribe,
    expect,
    iit,
    it
} from '@angular/core/testing';
import {PhotoListContext} from './photo-list-context.model';

describe('PhotoListContext', () => {
    /*
    it('should create an instance', () => {
      expect(new PhotoListContext()).toBeTruthy();
    });
    */
    expect(true).toBeTruthy();
});
