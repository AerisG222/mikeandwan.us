import {
    describe,
    ddescribe,
    expect,
    iit,
    it
} from '@angular/core/testing';
import {ContainerBox} from './container-box.model';

describe('ContainerBox', () => {
    /*
    it('should create an instance', () => {
      expect(new ContainerBox()).toBeTruthy();
    });
    */
    expect(true).toBeTruthy();
});
