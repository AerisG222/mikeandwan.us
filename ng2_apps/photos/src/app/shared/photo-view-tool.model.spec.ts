import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {PhotoViewTool} from './photo-view-tool.model';

describe('PhotoViewTool', () => {
  /*
  it('should create an instance', () => {
    expect(new PhotoViewTool()).toBeTruthy();
  });
  */
  expect(true).toBeTruthy();
});
