import { MawFocusRemoverDirective } from './maw-focus-remover.directive';

describe('FocusRemoverDirective', () => {
  it('should create an instance', () => {
    const directive = new MawFocusRemoverDirective();
    expect(directive).toBeTruthy();
  });
});
