{
  "name": "photo_stats",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "ts-loader": "^3.2.0",
    "typescript": "^2.6.2",
    "webpack": "^3.10.0"
  },
  "scripts": {
    "dev_build": "rm -rf dist && webpack",
    "prod_build": "rm -rf dist && webpack -p --config ./webpack.production.config.js"
  },
  "dependencies": {
    "d3": "^4.12.0",
    "oidc-client": "^1.4.1"
  }
}
