@model MawMvcApp.ViewModels.About.ContactModel
@{ 
    Layout = "_LayoutSidebar";
    ViewBag.Title = "about | contact"; 
}

@section scripts_head {
    @Html.Partial("_js_GoogleRecaptcha")
}

@section sidebar {
    @Html.Partial("_AboutSidebar", "contact")
}

@section header {
    @Html.Partial("_PageHeader", "Contact Us")
}

@if(Model.SubmitAttempted) {
	if(Model.SubmitSuccess) {
        <p maw-alert-type="Success">Thank you, your submission was successful.</p>
	}
	else {
        <p maw-alert-type="Danger">Sorry, there was an error submitting your request, please try again later.</p>
	}
}

@if(!Model.SubmitSuccess) {
    <p>Have something on your mind?  Let us know!</p>
    
    <form asp-controller="about" asp-action="contact" method="post" class="form-horizontal" autocomplete="off" role="form">
        <div class="form-group" maw-for="@Model.FirstName">
            <label asp-for="FirstName" class="col-sm-3 control-label"></label>
            <div class="col-sm-6">
                <input asp-for="FirstName" class="form-control" autofocus="" />
                <span asp-validation-for="FirstName" class="help-block"></span>
            </div>
    	</div>
        <div class="form-group" maw-for="@Model.LastName">
            <label asp-for="LastName" class="col-sm-3 control-label"></label>
            <div class="col-sm-6">
                <input asp-for="LastName" class="form-control" />
                <span asp-validation-for="LastName" class="help-block"></span>
            </div>
    	</div>
        <div class="form-group" maw-for="@Model.Email">
            <label asp-for="Email" class="col-sm-3 control-label"></label>
            <div class="col-sm-6">
                <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="help-block"></span>
            </div>
    	</div>
        <div class="form-group" maw-for="@Model.Message">
            <label asp-for="Message" class="col-sm-3 control-label"></label>
            <div class="col-sm-9">
                <textarea asp-for="Message" rows="10" class="form-control"></textarea>
                <span asp-validation-for="Message" class="help-block"></span>
            </div>
    	</div>
        
        <div class="form-group" maw-for="@Model.IsHuman">
            <div class="col-sm-offset-3 col-sm-6">
                @Html.Partial("_GoogleRecaptcha", @Model.RecaptchaSiteKey)
                <span asp-validation-for="IsHuman" class="help-block"></span>
            </div>
        </div>
    
        <div class="form-group">
            <div class="col-sm-offset-3 col-sm-2">
        		<button type="submit" class="btn btn-default">Submit</button>
        	</div>
        </div>
    </form>
}
