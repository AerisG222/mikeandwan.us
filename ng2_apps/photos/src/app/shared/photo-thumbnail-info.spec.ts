import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {PhotoThumbnailInfo} from './photo-thumbnail-info';

describe('PhotoThumbnailInfo', () => {
  /*
  it('should create an instance', () => {
    expect(new PhotoThumbnailInfo()).toBeTruthy();
  });
  */
  expect(true).toBeTruthy();
});
