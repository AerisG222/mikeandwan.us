import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {RatingPhotoSource} from './rating-photo-source';

describe('RatingPhotoSource', () => {
  /*
  it('should create an instance', () => {
    expect(new RatingPhotoSource()).toBeTruthy();
  });
  */
  expect(true).toBeTruthy();
});
