import {
    describe,
    ddescribe,
    expect,
    iit,
    it
} from '@angular/core/testing';
import {SelectedThumbnail} from './selected-thumbnail.model';

describe('SelectedThumbnail', () => {
    /*
    it('should create an instance', () => {
      expect(new SelectedThumbnail()).toBeTruthy();
    });
    */
    expect(true).toBeTruthy();
});
