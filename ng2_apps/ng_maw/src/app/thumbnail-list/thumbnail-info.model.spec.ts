import {
    describe,
    ddescribe,
    expect,
    iit,
    it
} from '@angular/core/testing';
import {ThumbnailInfo} from './thumbnail-info.model';

describe('ThumbnailInfo', () => {
    /*
    it('should create an instance', () => {
      expect(new ThumbnailInfo()).toBeTruthy();
    });
    */
    expect(true).toBeTruthy();
});
