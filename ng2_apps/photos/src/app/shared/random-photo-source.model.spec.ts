import {
    describe,
    ddescribe,
    expect,
    iit,
    it
} from '@angular/core/testing';
import {RandomPhotoSource} from './random-photo-source.model';

describe('RandomPhotoSource', () => {
    /*
    it('should create an instance', () => {
      expect(new RandomPhotoSource()).toBeTruthy();
    });
    */
    expect(true).toBeTruthy();
});
