import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {ModeRouteInfo} from './mode-route-info.model';

describe('ModeRouteInfo', () => {
  /*
  it('should create an instance', () => {
    expect(new ModeRouteInfo()).toBeTruthy();
  });
  */
  expect(true).toBeTruthy();
});
